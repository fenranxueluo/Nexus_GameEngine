<UserControl x:Class="NexusEditor.Editors.TransformView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NexusEditor.Editors"
             xmlns:vm="clr-namespace:NexusEditor.Components"
             xmlns:utl="clr-namespace:NexusEditor.Utilities.Controls"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:MSTransform, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource LightTextBlockStyle}" />
    </UserControl.Resources>

    <local:ComponentView Header="变换组件">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="62" />
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="19*" />
                <ColumnDefinition Width="123*"/>
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="位置" Grid.Column="0" />
            <utl:VectorBox X="{Binding PosX}" Y="{Binding PosY}" Z="{Binding PosZ}" Grid.Column="2" Grid.Row="0"
                           PreviewMouseLeftButtonDown="OnPosition_VectorBox_PreviewMouse_LBD"
                           PreviewMouseLeftButtonUp="OnPosition_VectorBox_PreviewMouse_LBU"
                           LostKeyboardFocus="OnPosition_VectorBox_LostKeyboardFocus" Grid.ColumnSpan="2"/>

            <TextBlock Text="旋转" Grid.Column="0"  Grid.Row="1" />
            <utl:VectorBox X="{Binding RotX}" Y="{Binding RotY}" Z="{Binding RotZ}" Grid.Column="2"
                           PreviewMouseLeftButtonDown="OnRotation_VectorBox_PreviewMouse_LBD"
                           PreviewMouseLeftButtonUp="OnRotation_VectorBox_PreviewMouse_LBU"
                           LostKeyboardFocus="OnRotation_VectorBox_LostKeyboardFocus" Grid.ColumnSpan="2" Margin="0,37,0,31" Grid.RowSpan="3"/>

            <TextBlock Text="缩放" Grid.Column="0"  Grid.Row="2" />
            <utl:VectorBox X="{Binding ScaleX}" Y="{Binding ScaleY}" Z="{Binding ScaleZ}" Grid.Column="2" Grid.Row="1"
                           PreviewMouseLeftButtonDown="OnScale_VectorBox_PreviewMouse_LBD"
                           PreviewMouseLeftButtonUp="OnScale_VectorBox_PreviewMouse_LBU"
                           LostKeyboardFocus="OnScale_VectorBox_LostKeyboardFocus" Grid.ColumnSpan="2" Margin="0,37,0,0" Grid.RowSpan="2"/>
        </Grid>
    </local:ComponentView>

</UserControl>